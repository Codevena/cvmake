{
  "name": "@codevena/cvmake-showcase",
  "version": "0.0.0",
  "private": true,
  "description": "Static showcase landing page deployed to GitHub Pages.",
  "scripts": {
    "sync-assets": "rm -rf screenshots && mkdir -p screenshots && cp ../../docs/screenshots/*.png screenshots/",
    "build:css": "tailwindcss -i src/input.css -o tailwind.css --minify",
    "build": "pnpm run build:css",
    "dev": "pnpm run sync-assets && pnpm run build:css && python3 -m http.server 4173"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.0",
    "tailwindcss": "4.3.0"
  }
}
